# Vite reads VITE_-prefixed variables and inlines them at build time.
# Copy to `.env` (gitignored via *.local conventions) to override for
# host dev, or pass via docker-compose `build.args` for container dev.

# Base URL the client uses to reach the auth-service `/greet` endpoint.
VITE_AUTH_API=http://localhost:8081
