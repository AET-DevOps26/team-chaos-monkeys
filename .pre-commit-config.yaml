# Local secret scanning before every commit (issue #286).
# Install once: `pre-commit install` (see README "Secret scanning").
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
