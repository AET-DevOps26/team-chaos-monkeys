.superpowers/
docs/superpowers/plans/
docs/superpowers/specs/

# Per-developer compose customization (port overrides, debug mounts, etc.).
# Copy docker-compose.override.yml.example to start.
docker-compose.override.yml

**/build/
**/bin/
**/.gradle/
**/bin/
!**/src/main/**/bin/
!**/src/test/**/bin/
.env
.env.*
!.env.example

# macOS
**/.DS_Store

# Claude Code workspace state
.claude/

# VS Code devcontainer auto-generated lock
.devcontainer/devcontainer-lock.json

# Local-only personal notes / task scratchpad
tasks/
meeting-notes.*

# Python uv lockfile — not consumed by Docker/CI (both use `pip install`
# from pyproject.toml). Promote to tracked when wiring `uv sync --frozen`.
services/genai-service/uv.lock

# Local-only helm secrets (see docs/deployment/local-kubernetes.md)
infra/helm/foundflow/values-local.yaml.local

# helm output rendered by the iac-scan CI job (contains generated secrets)
rendered-manifests/
